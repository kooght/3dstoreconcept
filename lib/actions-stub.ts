"use client";

export async function createProduct(_formData: FormData) {
  alert(
    "Création désactivée sur la démo GitHub Pages. Lancez l'app en local (npm run dev) pour modifier les produits."
  );
}

export async function updateProduct(_id: string, _formData: FormData) {
  alert(
    "Modification désactivée sur la démo GitHub Pages. Lancez l'app en local (npm run dev) pour modifier les produits."
  );
}
